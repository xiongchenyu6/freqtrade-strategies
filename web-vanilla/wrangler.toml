#:schema node_modules/wrangler/config-schema.json
# Cloudflare Pages config — deploy via `wrangler pages deploy`
name = "quant"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./public"

# Account is passed via CLOUDFLARE_ACCOUNT_ID env var (wrangler config rejects it here).
# Account: Xiongchenyu6@gmail.com's Account = 2764ae0fd9a5cb92c9ac67708620e54c

# Environment variables exposed to the browser bundle (public — NO SECRETS)
[vars]
API_BASE       = "https://api.panda.qzz.io"
AUTH_BASE      = "https://auth.panda.qzz.io"
SUPABASE_URL   = "https://rhweqsxothaezsbxjwaj.supabase.co"
SUPABASE_ANON  = "sb_publishable_RRSWxhXvvaUqk3S9nF7m9A_loMqoxxw"

# Secrets for Pages Functions (server-side only) — set via:
#   wrangler pages secret put <NAME> --project-name quant
# They arrive in functions as env.<NAME>.
# Examples:
#   SUPABASE_SERVICE_ROLE_KEY  — if we ever need server-side bypass of RLS
#   TIMESCALE_ADMIN_URL        — if we need DB-admin-level queries (don't do this unless truly necessary)
