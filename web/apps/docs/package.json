{
	"name": "@quant/docs",
	"type": "module",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "node scripts/sync-docs.mjs && astro dev",
		"build": "node scripts/sync-docs.mjs && astro build",
		"preview": "astro preview",
		"astro": "astro",
		"deploy": "node scripts/sync-docs.mjs && astro build && wrangler pages deploy dist --project-name=quant-docs --commit-dirty=true --branch=main"
	},
	"dependencies": {
		"@astrojs/starlight": "^0.38.3",
		"astro": "^6.0.1",
		"sharp": "^0.34.2"
	}
}
